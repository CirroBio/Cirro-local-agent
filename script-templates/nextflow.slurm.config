workDir = "${PW_LOCAL_WORK}"
process.executor = "slurm"
process.clusterOptions = "--priority=\"${PW_WORKER_PRIORITY:-5}\""

apptainer {
    enabled = true
    autoMounts = true
    cacheDir = "${PW_APPTAINER_CACHEDIR}"
    runOptions = '--containall -B \$TMPDIR -W \$TMPDIR'
}
